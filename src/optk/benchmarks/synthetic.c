// #include "synthetic.h"

typedef enum { linear, categorical } param;

param my_lin_param = linear;


typedef struct {
    int opt; 
} space;


typedef int dim;     // problem dimension
typedef int Ub;      // upper bound
typedef int Lb;      // lower bound
typedef double opt;  // optimal solution


double ackley_1 (double x, dimension) {
    
}

